git remote add origin https://github.com/TchunChao/veille-node.git
git push -u origin master 
	//Local vers distant

git add <fichier>
git add .

git status
git status -s

git commit -m "<commentaire>"


//Branches
git branche <nom pour la branche>
git push origin <branche>
git checkout <branche>